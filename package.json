{
  "name": "time",
  "private": true,
  "workspaces": [
    "worker",
    "site"
  ],
  "scripts": {
    "dev": "yarn turbo run dev",
    "deploy:worker": "yarn workspace worker deploy"
  },
  "dependencies": {
    "turbo": "1.3.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^3.14.0",
    "@types/node": "^14",
    "@types/react": "^18",
    "typescript": "^4",
    "wrangler": "^2.0.15"
  }
}
