{
  "name": "time",
  "private": true,
  "workspaces": [
    "worker",
    "site"
  ],
  "scripts": {
    "dev": "yarn turbo run dev",
    "deploy:worker": "yarn workspace worker deploy",
    "build-site": "yarn turbo run build",
    "start-site": "yarn turbo run start"
  },
  "devDependencies": {
    "turbo": "1.3.1"
  },
  "packageManager": "yarn@3.2.1"
}
