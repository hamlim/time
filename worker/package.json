{
  "name": "worker",
  "scripts": {
    "dev": "yarn wrangler dev",
    "deploy": "yarn wrangler publish"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^3.14.0",
    "wrangler": "^2.0.15"
  }
}
